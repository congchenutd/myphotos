#include "ToolBar.h"
#include <QHBoxLayout>
#include <QLabel>
#include <QRadioButton>

ToolBar::ToolBar(QWidget* parent)
    : QToolBar(parent)
{
//    _radioName = new QRadioButton(tr("Name"), this);
//    QHBoxLayout* layout = new QHBoxLayout(this);
//    layout->addWidget(new QLabel(tr("  Sort by: "), this));
//    this->layout()->addWidget(_radioName);
}
